/**
 * `cr-card-property`
 * This element represents the property of a Clash Royale card.
 *
 * It is meant to be used only inside a `cr-card` element, so it is quite useless by itself.
 *
 * @customElement
 */
export class CRCardProperty extends HTMLElement {
}
